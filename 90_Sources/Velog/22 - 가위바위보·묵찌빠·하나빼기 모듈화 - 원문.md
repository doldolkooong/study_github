---
type: source
source_url: "https://velog.io/@doldolkoong/%EA%B0%80%EC%9C%84%EB%B0%94%EC%9C%84%EB%B3%B4-%EB%AC%B5%EC%B0%8C%EB%B9%A0-%ED%95%98%EB%82%98%EB%B9%BC%EA%B8%B0-%EB%AA%A8%EB%93%88%ED%99%94"
source_title: "가위바위보, 묵찌빠, 하나빼기 모듈화"
source_id: 1a1ab3b5-7b60-4ff3-962b-ad765c87ab67
source_author: doldolkoong
source_published: 2026-08-16
source_updated: 2026-09-01
archived: 2026-09-08
tags:
  - velog-source
---

# 가위바위보, 묵찌빠, 하나빼기 모듈화

[Velog 원문](https://velog.io/@doldolkoong/%EA%B0%80%EC%9C%84%EB%B0%94%EC%9C%84%EB%B3%B4-%EB%AC%B5%EC%B0%8C%EB%B9%A0-%ED%95%98%EB%82%98%EB%B9%BC%EA%B8%B0-%EB%AA%A8%EB%93%88%ED%99%94) · [[가위바위보·묵찌빠·하나빼기 모듈화|가위바위보·묵찌빠·하나빼기 모듈화]]

> [!info] 원문 보관
> 2026-09-08 수집한 공개 본문이다. 원문의 설명·코드·표기 오류도 보존했으며 학습 노트의 보완란과 함께 읽는다. 이미지는 원문 외부 링크를 유지하여 인터넷 연결이 필요하다.

---

강사님께서 숙제로 내주신 가위바위보, 묵찌빠, 하나빼기를 모듈화 하여 구현한 내용이다. 

**<파일 구성>**

>2. comprehension game/
│
└── game/
    ├── __pycache__/
    ├── constants.py
    ├── game_choice.py
    ├── inputs.py
    ├── mukchippa.py
    ├── one_more.py
    ├── prints.py
    ├── rock_sissor_paper.py
    │
    └── main.py
    
<br>

**constants.py**
각 파일들에 사용될 상수 선언

```python
from __future__ import annotations
import enum

# 가위 바위 보 저장한 상수
# 가위 바위 보 낸 것을 토대로 게임 결과를 저장하는 함수 생성
class rock_sissor_paper(enum.Enum):
    가위 = 1
    바위 = 2
    보 = 0

    def is_win(self, computer : rock_sissor_paper) -> str:
        result = (self.value - computer.value) % 3

        return game_result(result).name

# 게임 결과를 저장한 상수 game_result
# 1이면 이김, 2면 짐, 0이면 비김
class game_result(enum.Enum):
    win = 1 
    lose = 2
    draw = 0

# 게임 종류 상수
class game_type(enum.Enum):
    가위바위보 = enum.auto()
    하나빼기 = enum.auto()
    묵찌빠 = enum.auto()

# 묵찌빠 공격권 괸련 상수
class mukchippa_attack(enum.Enum):
    user_attack = 1
    user_shield = 0

	# 가위바위보 이겼으면 사용자 공격, 졌으면 사용자 수비
    def get_attacker(self, result):
        if result == game_result.win.name:
            return self.user_attack
        elif result == game_result.lose.name:
            return self.user_shield
        return self
```  
<br>


**game_choice.py**
가위바위보, 하나빼기, 묵찌빠 중 원하는 게임 종류룰 선택할 수 있는 파일

``` python
from .constants import game_type

def game_choice() -> game_type:
    user_game_choice = input("진행하고 싶은 게임을 골라주세요 : ").strip()

    if user_game_choice not in game_type.__members__:
        print("가위바위보, 묵찌빠, 하나빼기 중 골라주세요")
        return game_choice()

    return game_type[user_game_choice]

if __name__ == "__main__":
    print(game_choice())
```

<br>

**input.py**
사용자의 입력값을 받을 수 있는 파일

```python
import random
from .constants import rock_sissor_paper

def get_user_input() -> rock_sissor_paper:
    user_input = input("가위, 바위, 보 중 하나를 선택하세요").strip()
	
    # 사용자가 가위, 바위 보 중 안 골랐을 경우
    if user_input not in rock_sissor_paper.__members__:
        print("다시 선택해주세요..^^;;")
        return get_user_input()

    return rock_sissor_paper[user_input]


if __name__ == "__main__":
    print(get_user_input())
```


<br>

**mukchippa.py**
묵찌빠 게임이 구현된 파일

```python
from .constants import rock_sissor_paper
import random
from .inputs import get_user_input
from .constants import mukchippa_attack

def mukchippa_game() -> dict:
    user_choice = get_user_input()
    computer_choice = random.choice(list(rock_sissor_paper.__members__))

    print(f"User 선택: {user_choice.name}") # 사용자의 선택 출력
    print(f"Computer 선택: {computer_choice}") # 컴퓨터의 선택 출력

    result = user_choice.is_win(rock_sissor_paper[computer_choice])
    attacker = mukchippa_attack.user_shield
    attacker = attacker.get_attacker(result)

    while True: # 공격권 유무 확인

        if attacker == mukchippa_attack.user_attack:
            print("User은 공격을 하셔야 합니다.")
        else:
            print("User은 수비를 하셔야 합니다.")

        user_choice = get_user_input()
        computer_choice = random.choice(list(rock_sissor_paper.__members__))

        if user_choice.name == computer_choice:
            if attacker == mukchippa_attack.user_attack:
                return{
                        "게임 결과" : "승리",
                        "User 선택" : user_choice.name,
                        "Computer 선택" : computer_choice
                }
            else:
                return{
                        "게임 결과" : "패배",
                        "User 선택" : user_choice.name,
                        "Computer 선택" : computer_choice
                }
            
        print(f"User 선택: {user_choice.name}") # 사용자의 선택 출력
        print(f"Computer 선택: {computer_choice}") # 컴퓨터의 선택 출력

        result = user_choice.is_win(rock_sissor_paper[computer_choice])
        attacker = attacker.get_attacker(result)
```

<br>

**one_more.py**
하나빼기가 구현된 파일

```python
from .constants import rock_sissor_paper
import random
from .inputs import get_user_input

def one_more_game() -> dict:

    print("==가위 바위 보!==")

    user_choice = [] # 사용자의 선택을 리스트에 담았다.
    for i in range(2):
        user_choice.append(get_user_input())
    computer_choice = random.choices(list(rock_sissor_paper.__members__), k = 2)

    print(f"User 선택: {[choice.name for choice in user_choice]}") # 사용자의 선택 출력
    print(f"Computer 선택: {computer_choice}") # 컴퓨터의 선택 출력 

    while True: # 최종 선택 시 리스트 내에서 선택할 수 있게 했다.
        final_user_choice = input(f"{[choice.name for choice in user_choice]} 중 하나 골라주세요.").strip()
        
        final_computer_choice = random.choice(list(computer_choice))

        print("~하나 빼기!~")

        if final_user_choice not in [choice.name for choice in user_choice]:
            print("다시 선택해주세요..^^;;")
            continue

        final_user_choice = rock_sissor_paper[final_user_choice]

        print(f"<ser의 선택: {final_user_choice.name}")
        print(f"computer의 선택 : {final_computer_choice}")

        return{
            "게임 결과" : final_user_choice.is_win(rock_sissor_paper[final_computer_choice]),
            "User 선택" : final_user_choice.name,
            "Computer 선택" : final_computer_choice
        }
        
        
```

<br>

**prints.py**
게임 결과를 출력하는 파일
```python
def print_game_result(result:str, user_value:str=None, computer_value:str=None) -> None:
    if user_value and computer_value:
        print(f"User 선택 : {user_value} \nComputer 선택 : {computer_value}")

    print(f"게임 결과 :  {result}")
```

<br>

**rock_sissor_paper.py**
가위바위보가 구현되어있는 파일

```python
from .constants import rock_sissor_paper
import random
from .inputs import get_user_input

def rsp_game() -> dict:
    user_choice = get_user_input()
    computer_choice = random.choice(list(rock_sissor_paper.__members__))

    return{
        "게임 결과" : user_choice.is_win(rock_sissor_paper[computer_choice]),
        "User 선택" : user_choice.name,
        "Computer 선택" : computer_choice
    }
```

<br>

**main.py**
게임이 실행되게 해주는 파일

```python
from game.constants import game_type
from game.prints import print_game_result
from game.rock_sissor_paper import rsp_game
from game.game_choice import game_choice
from game.mukchippa import mukchippa_game
from game.one_more import one_more_game

def run_game():
    game_style = game_choice()
    game_result = None

    # 선택한 게임
    if game_style == game_type.가위바위보:
        game_result = rsp_game()

    elif game_style == game_type.하나빼기:
        game_result = one_more_game()

    elif game_style == game_type.묵찌빠:
        game_result = mukchippa_game()


    if game_result:
        print_game_result(
            result = game_result['게임 결과'],
            user_value = game_result['User 선택'],
            computer_value = game_result['Computer 선택']
        )

if __name__ == "__main__":
    run_game()
```

<br>

** 실행 사진 **

- 가위바위보
![](https://velog.velcdn.com/images/doldolkoong/post/150bc4a4-c3c4-420e-8690-682a6e2d4752/image.png)

<br>

- 하나빼기
![](https://velog.velcdn.com/images/doldolkoong/post/858fdd27-5e12-4435-8b32-442bfffdd316/image.png)

<br>

- 묵찌빠
![](https://velog.velcdn.com/images/doldolkoong/post/e98bdc4d-2397-45e9-bbe5-261e0fc0f3f1/image.png)

